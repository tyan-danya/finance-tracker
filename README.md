# Траты (SpendTracker)

Android-приложение для учёта личных расходов: быстрый ввод трат, дерево категорий,
статистика с графиками, фильтры по периодам и экспорт данных — в том числе готовый
отчёт, который можно отдать языковой модели с просьбой оптимизировать расходы.

Всё хранится локально на устройстве. Никаких аккаунтов, сети и аналитики.

## Возможности

- **Ввод траты**: сумма, категория, подкатегория, дата покупки (можно задним числом),
  способ оплаты, комментарий. Быстрые чипы «Сегодня / Вчера / Позавчера».
- **Категории**: 17 предзаполненных категорий и ~90 подкатегорий из коробки.
  Свои категории и подкатегории добавляются прямо из формы ввода.
  Используемые категории нельзя удалить — только скрыть, чтобы не рвать историю.
- **Статистика**: сумма за период, сравнение с прошлым периодом, средний чек,
  медиана, средние траты в день; кольцевая диаграмма и рейтинг по категориям;
  динамика по дням и по месяцам; разрез по подкатегориям, дням недели и способам оплаты;
  сводка «куда уходит больше всего».
- **Фильтры**: месяц, конкретный день, год, произвольный период, всё время.
- **Автоучёт из уведомлений банков**: приложение разбирает пуши банковских приложений
  (и, по желанию, банковские СМС) и складывает операции в раздел «Черновики».
  В траты и статистику они попадают **только после подтверждения**.
- **Экспорт**: CSV (Excel), JSON (полные данные) и markdown-отчёт для ИИ-анализа.

## Автоучёт из уведомлений

Как это работает:

1. При первом запуске приложение спрашивает, включать ли автоучёт. Можно пропустить —
   и включить позже во вкладке **Ещё → Траты из уведомлений**.
2. Доступ к уведомлениям выдаётся в системных настройках Android (иначе нельзя —
   такое разрешение не запрашивается диалогом). Приложение открывает нужный экран само.
3. Читаются **только приложения банков из списка** в настройках, каждое включается отдельно.
   Остальные уведомления не разбираются вовсе.
4. Распознанная операция попадает в **«Черновики»** — отдельную вкладку со счётчиком.
   Категория подставляется по истории ваших подтверждений, затем по словарю известных сетей;
   если не подобралась — приложение честно просит выбрать её, а не ставит «Прочее» молча.
5. Приложение показывает своё уведомление о новой операции с кнопками «Подтвердить» и
   «Отклонить» — разобрать трату можно, не открывая приложение. Это отключаемо.
6. В «Черновиках» операции сгруппированы по дням: можно подтвердить по одной, поправить
   сумму/дату/категорию перед подтверждением, подтвердить все готовые разом или отклонить.
   Операции, похожие на уже существующие траты, помечаются как возможные дубликаты.

Что важно понимать:

- **Ничего не создаётся молча.** Пока операция не подтверждена, она не влияет ни на
  статистику, ни на экспорт — она физически лежит в отдельной таблице.
- **Данные не покидают устройство**: у приложения по-прежнему нет разрешения на интернет.
- **Тексты пушей у банков меняются**, поэтому парсер устроен как набор признаков
  (ключевое слово операции, сумма, маска карты, мерчант) и покрыт тестами на корпусе
  реальных текстов — почините правило, добавив случай в `NotificationParserTest`.

## Установка APK

Готовый файл — в каталоге `dist/`. Скопируйте его на телефон и откройте;
Android спросит разрешение на установку из неизвестного источника — это ожидаемо,
приложение подписано локальным ключом, а не через Google Play.

## Сборка из исходников

Нужны JDK 17 и Android SDK (compileSdk 35, build-tools 35.0.0).

В этом окружении тулчейн развёрнут локально в `G:\toolchain`, и есть скрипт-обёртка,
который сам подставляет `JAVA_HOME` / `ANDROID_HOME`:

```bash
./build.ps1 test
```

```bash
./build.ps1 assembleRelease
```

Без обёртки — обычный Gradle wrapper, при заданных `JAVA_HOME` и `local.properties`:

```bash
./gradlew test assembleRelease
```

Release-сборка подписывается ключом из `keystore/spendtracker.jks`
(пароль хранилища и ключа — `spendtracker`, алиас — `spendtracker`).
Этот ключ пригоден только для личного использования: не публикуйте с ним приложение
в сторах и не переиспользуйте его в других проектах.

## Архитектура

```
domain/model      модели (ExpenseRecord, Category, Period, PendingOperation) — чистый Kotlin
domain/stats      StatsCalculator и модели агрегатов — чистый Kotlin, покрыт тестами
domain/MoneyFormat форматирование и парсинг сумм (всё в копейках, Long)
data/db           Room: сущности, DAO, база
data/             ExpenseRepository — единая точка доступа к данным, SettingsStore, DefaultCategories
importer/         разбор выписки Т-Банка (CSV)
notifications/    разбор пушей банков, слушатель уведомлений, своё уведомление о новой операции
export/           CSV / JSON / markdown-отчёт, ExportManager (FileProvider + share)
ui/               Compose-экраны, графики на Canvas, тема
```

Ключевые решения:

- **Деньги — только `Long` в копейках.** Никаких `Double`, поэтому суммы не «плывут».
- **Даты — `LocalDate`, в базе хранятся как `epochDay`.** Нет таймзонных сдвигов,
  сортировка работает на уровне индекса.
- **Ручной DI** (`AppContainer`) вместо Hilt — компонентов мало, сборка быстрее.
- **Графики нарисованы вручную на Compose Canvas** — без сторонних чарт-библиотек,
  меньше зависимостей и полный контроль над темизацией.
- **Внешние ключи включены** (`PRAGMA foreign_keys = ON`): категория с тратами защищена
  от удаления (`RESTRICT`), удаление подкатегории обнуляет ссылку (`SET NULL`).
- **Неподтверждённые операции — в отдельной таблице** `pending_operations`, а не в `expenses`
  со статусом. Так ни один существующий запрос списка, статистики и экспорта их не видит,
  и черновики не могут «протечь» в расчёты.
- **Парсер уведомлений — чистая функция** `(packageName, title, text, time) -> ParsedNotification?`
  без Android-зависимостей: правила чинятся тестами, а не запуском на телефоне.

## Тесты

```bash
./build.ps1 test
```

Покрыты: `StatsCalculator` (агрегаты, периоды, zero-fill рядов, граничные даты),
`Period` / `DateRange`, `MoneyFormat` (парсинг пользовательского ввода),
экспортеры (экранирование CSV, валидность JSON, содержимое отчёта),
импорт выписки, `NotificationParser` на корпусе текстов уведомлений банков,
а также слой Room через Robolectric (репозиторий, очередь подтверждения, миграции 1→2 и 2→3,
ограничения внешних ключей, уникальность).

## Что дальше

- [docs/roadmap.md](docs/roadmap.md) — план развития.
- [docs/bank-integration.md](docs/bank-integration.md) — проработка интеграции с банками:
  способы, ограничения, влияние на модель данных. Фаза 1 (импорт выписки) и Фаза 2
  (парсер уведомлений с подтверждением) реализованы; Фаза 3 (API/агрегаторы) — вне планов.
